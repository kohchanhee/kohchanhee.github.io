# Assets

Use this folder for UI assets imported by React components, such as small textures, icons, or decorative images that should be bundled and cache-busted by Vite.

For portfolio content like project screenshots and videos, use `public/media` so the files have stable public paths.
